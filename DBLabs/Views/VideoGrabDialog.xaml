<Window x:Class="DBLabs.Views.VideoGrabDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Grab Frame from Video" Height="290" Width="520"
        WindowStartupLocation="CenterOwner" ResizeMode="NoResize"
        Background="{StaticResource AppBackground}" FontFamily="Segoe UI">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Text="Video file or stream URL" Foreground="{StaticResource TextSecondary}" Margin="0,0,0,4"/>
        <Grid Grid.Row="1" Margin="0,0,0,14">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <TextBox x:Name="SourceBox" Style="{StaticResource InputBox}" Margin="0,0,8,0"/>
            <Button Grid.Column="1" Content="Browse…" Width="90" Height="32" Click="Browse_Click" Style="{StaticResource ToolButton}"/>
        </Grid>

        <TextBlock Grid.Row="2"
                   Text="Several frames from across the video are decoded and scored for sharpness at the same time. The first one that is clearly in focus is used — blurred and motion-smeared frames are skipped."
                   Foreground="{StaticResource TextSecondary}" FontSize="11" TextWrapping="Wrap" Margin="0,0,0,12"/>

        <TextBlock Grid.Row="3" x:Name="StatusText" Text="" Foreground="{StaticResource TextSecondary}"
                   FontSize="12" TextWrapping="Wrap" Visibility="Collapsed" Margin="0,0,0,8"/>

        <StackPanel Grid.Row="5" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="Cancel" Width="90" Height="32" Margin="0,0,8,0" Click="Cancel_Click" Style="{StaticResource ToolButton}"/>
            <Button x:Name="GrabButton" Content="Grab Sharp Frame" Width="150" Height="32" Click="Grab_Click" Style="{StaticResource PrimaryButton}"/>
        </StackPanel>
    </Grid>
</Window>
