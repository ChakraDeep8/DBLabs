<Window x:Class="DBLabs.Views.NameShapeDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Name this region" Height="200" Width="420"
        WindowStartupLocation="CenterOwner" ResizeMode="NoResize"
        Background="{StaticResource AppBackground}" FontFamily="Segoe UI">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock x:Name="PromptText" Text="Name this region" Foreground="{StaticResource TextPrimary}" FontSize="14" Margin="0,0,0,10"/>
        <TextBox x:Name="NameBox" Grid.Row="1" Style="{StaticResource InputBox}" FontSize="14" Padding="8,7"/>
        <TextBlock x:Name="ExampleText" Grid.Row="2" Text="Example: CUSTOMER_ZONE_ROI" Foreground="{StaticResource TextSecondary}" FontSize="11" Margin="0,6,0,0"/>
        <TextBlock x:Name="ErrorText" Grid.Row="3" Foreground="{StaticResource ErrorBrush}" FontSize="11" TextWrapping="Wrap" VerticalAlignment="Bottom" Visibility="Collapsed"/>

        <StackPanel Grid.Row="4" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,14,0,0">
            <Button Content="Cancel" Width="90" Height="32" Margin="0,0,8,0" Click="Cancel_Click" Style="{StaticResource ToolButton}" IsCancel="True"/>
            <Button Content="Create" Width="90" Height="32" Click="Create_Click" Style="{StaticResource ToolButton}" IsDefault="True"/>
        </StackPanel>
    </Grid>
</Window>
